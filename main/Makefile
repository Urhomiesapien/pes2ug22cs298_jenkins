# Build the hello_exec executable from hello.c
hello_exec: hello.c
	gcc -o hello_exec hello.c  # Uses TAB (not spaces) for indentation

# Clean/remove compiled artifacts
clean:
	rm -f hello_exec  # Uses TAB (not spaces) for indentation
